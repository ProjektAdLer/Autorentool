@using Microsoft.Extensions.Localization
<MudDialog>
    <DialogContent>
        @Localizer["Dialog.Content.Text", @WorldName]
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Yes">@Localizer["Dialog.Button.Yes"]</MudButton>
        <MudButton OnClick="No">@Localizer["Dialog.Button.No"]</MudButton>
        <MudButton OnClick="Cancel">@Localizer["Dialog.Button.Cancel"]</MudButton>
    </DialogActions>
</MudDialog>
@code {
    [CascadingParameter]
    public MudDialogInstance MudDialog { get; set; }
    [Parameter, EditorRequired]
    public string WorldName { get; set; }
    
    [Inject]
    private StringLocalizer<UnsavedWorldDialog> Localizer { get; set; }
    
    
    private void Yes() => MudDialog.Close(DialogResult.Ok(true));
    private void No() => MudDialog.Close(DialogResult.Ok(false));
    private void Cancel() => MudDialog.Cancel();
}