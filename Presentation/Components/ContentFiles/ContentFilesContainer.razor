@*<header class="m-2 rounded-lg">
    <div class="flex justify-center p-2">
        <h2 class="text-xl text-center font-bold text-adlergrey-500 mx-2">Import of all files</h2>
    </div>
</header>*@
<MudPaper Class="w-full max-h-full flex p-3 overflow-auto">
    <CascadingValue Value="this">
        <div class="flex flex-col w-full grow">
            <div class="">
                <ContentFilesAdd/>
            </div>
            <MudDivider Class="max-h-[10px]"/>
            <div class="grow shrink w-full flex flex-col">
                <ContentFilesView @ref="ContentFilesView"/>
            </div>
        </div>
    </CascadingValue>
</MudPaper>

@code {
    private ContentFilesView ContentFilesView { get; set; }

    /// <summary>Rerenders this component as well as the <see cref="ContentFilesView"/> inside of it.</summary>
    internal async Task RerenderAsync()
    {
        await InvokeAsync(StateHasChanged);
        await ContentFilesView.RerenderAsync();
    }

}